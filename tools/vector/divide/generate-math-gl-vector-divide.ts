import { autoGeneratedFileComment } from '../../misc/auto-generated-file-comment.js';

export interface IGenerateMathGLVectorDivideOptions {
  readonly name: string;
  readonly length: number;
}

export function generateMathGLVectorDivide({
  name,
  length,
}: IGenerateMathGLVectorDivideOptions): string {
  return [
    ...autoGeneratedFileComment(),
    `import { ${name}, readonly_${name} } from './${name}.type.js';`,
    ``,
    `/**`,
    ` * Divides two ${name}'s.`,
    ` */`,
    `export function ${name}_divide<GType extends ArrayLike<number>>(`,
    `  out: ${name}<GType>,`,
    `  a: readonly_${name},`,
    `  b: readonly_${name},`,
    `): ${name}<GType> {`,
    ...Array.from({ length }, (_, index: number): string => {
      return `  out[${index}] = a[${index}] / b[${index}];`;
    }),
    `  return out;`,
    `}`,
    ``,
  ].join('\n');
}
